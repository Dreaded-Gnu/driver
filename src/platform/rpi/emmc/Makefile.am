
AUTOMAKE_OPTIONS = subdir-objects

AM_CFLAGS = -imacros ${top_builddir}/src/config.h
AM_CCASFLAGS = -imacros ${top_builddir}/srcinclude/config.h

bin_PROGRAMS = emmc.elf
emmc_elf_SOURCES = \
  main.c
